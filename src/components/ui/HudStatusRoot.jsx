import React from 'react';
import { HudStatus } from './HudStatus';

export const HudStatusRoot = () => {
  // Mantido no projeto; removemos sua renderização do App.jsx para não duplicar UI de health.
  return <HudStatus />;
};


